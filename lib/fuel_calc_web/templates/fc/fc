<div class="container">
  <div class="default-block">
    <h3 class="page-header">Calculate required fuel amount for route(-s):</h3>
    <br>
    <div>
    </div>
    <ul class="settings-list">
      <li onclick="displayOrHide('route_config')">Planned Routes:</li>
      <hr>
      <div id="route_config">
        <ul style="list-style-type:none; text-align: left; margin: 0;">
          <%= form_for @routes, @action, fn f -> %>
          <%= hidden_input f, :routes, [name: "routes"] %>
          <%= for {%{"by" => by, "launch" => launch, "landing" => landing}, i} <- Enum.with_index(@routes) do %>
            <li id="route_<%=i%>">
              <select name="routes[by]" required>
                <option><%= by %></option>
                <option>Apollo_11</option>
                <option>Mars_Mission</option>
                <option>Passenger_Ship</option>
              </select>
              <select name="routes[launch]" required>
                <option><%= launch %></option>
                <option>Earth</option>
                <option>Mars</option>
                <option>Moon</option>
              </select>
              <select name="routes[landing]" required>
                <option><%= landing %></option>
                <option>Earth</option>
                <option>Mars</option>
                <option>Moon</option>
              </select>
              <img class="intext-icon"
                   style="margin-left: 2.0rem;"
                   onclick="remove('routes_list', 'route_<%=i%>')"
                   src="<%=Routes.static_path(@conn, "/images/remove_circle.svg")%>">
            </li>
          <% end %>
          <li id="new_route">
              <span style="cursor: pointer; color: #0069d9;"
                    onclick="appendTime('<%= Routes.static_path(@conn, "/images/remove_circle.svg")%>')">
                <img class="intext-icon" src="<%= Routes.static_path(@conn, "/images/add_circle.svg")%>">add new
              </span>
          </li>
        </ul><%= submit "CALCULATE" %>
        <% end %>
      </div>
    </ul>    
  </div>
</div>